<!DOCTYPE html>
<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Дошка оголошень</title>

    <!-- Bootstrap core CSS -->
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    
    <!-- Custom styles for this template -->
    <link href="~/Content/PageStyle.css" rel="stylesheet" />
</head>

<body>

    <!-- Navigation -->
    <nav class="navbar fixed-top navbar-expand-lg navbar-dark  fixed-top">
        <div class="container">
            <a class="navbar-brand" href=#>OBYAVA</a>
             <div class="collapse navbar-collapse" id="navbarResponsive">
                 <ul class="navbar-nav ml-auto">
                     <li class="nav-item">
                         @Html.ActionLink("Головна", "List", "Ad", new { returnUrl = Request.Url.PathAndQuery }, new { @class = "nav-link" })
                     </li>
                     <li class="nav-item">
                         @Html.ActionLink("Зареєструватись", "Create", "Admin", new { returnUrl = Request.Url.PathAndQuery }, new { @class = "nav-link" })
                     </li>
                     <li class="nav-item dropdown">
                         <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownPortfolio" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Керування профілем
                         </a>
                         <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownPortfolio">
                             @Html.ActionLink("Увійти", "Login", "Account", new { returnUrl = Request.Url.PathAndQuery }, new { @class = "dropdown-item" })
                             @Html.ActionLink("Редагувати", "", "", new { returnUrl = Request.Url.PathAndQuery }, new { @class = "dropdown-item" })
                             @Html.ActionLink("Вийти", "", "", new { returnUrl = Request.Url.PathAndQuery }, new { @class = "dropdown-item" })

</li>
                     <li class="nav-item dropdown">
                         <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownPortfolio" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                             Оголошення
                         </a>
                         <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownPortfolio">
                             @Html.ActionLink("Розмістити", "Create", "Ad", new { returnUrl = Request.Url.PathAndQuery }, new { @class = "dropdown-item" })
                             @Html.ActionLink("Редагувати", "Change", "Ad", new { returnUrl = Request.Url.PathAndQuery }, new { @class = "dropdown-item" })
                             @Html.ActionLink("Видалити", "Change", "Ad", new { returnUrl = Request.Url.PathAndQuery }, new { @class = "dropdown-item" })

                         </div>
                     </li>
                     @Html.ActionLink("Розмістити оголошеня", "Create", "Ad",
                     new { returnUrl = Request.Url.PathAndQuery },
                     new { @class = "btn btn-success" })

                 </ul>
            </div>
        </div>
    </nav>

    
        <!-- Page Content -->
        <div class="container content">
            @RenderBody()
        </div>
    

    <footer class="py-3 fixed-bottom ">
        <div class="container">
            <p class="m-0 text-center text-white">Copyright &copy; OBYAVA 2020</p>
        </div>
        <!-- /.container -->
    </footer>

    <!-- Bootstrap core JavaScript -->

    <script src="~/Scripts/jquery-3.5.0.min.js"></script>
    <script src="~/Scripts/bootstrap.bundle.min.js"></script>

</body>





